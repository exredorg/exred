// part of the editor where the flow diagrams are
// this element holds the flow data (node instances and connections)

import Component from '@ember/component';

export default Component.extend({
});
